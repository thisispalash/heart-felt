import { Button } from "@chakra-ui/react";


export default function EnterSiteButton({ }) {

  const enterSite = async () => {}

  return (
    <Button
      size='lg'
      variant='outline'
      onClick={enterSite}
    >
      Enter Site
    </Button>
  );
}